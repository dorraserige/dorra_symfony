<!DOCTYPE html>
<html>
<head>
    <title>Livres publiés entre {{ startDate|date('Y') }} et {{ endDate|date('Y') }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1>Livres publiés entre {{ startDate|date('d/m/Y') }} et {{ endDate|date('d/m/Y') }}</h1>
        
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Titre</th>
                    <th>Date de publication</th>
                    <th>Catégorie</th>
                    <th>Auteur</th>
                </tr>
            </thead>
            <tbody>
                {% for book in books %}
                <tr>
                    <td>{{ book.title }}</td>
                    <td>{{ book.publicationYear ? book.publicationYear|date('d/m/Y') : 'N/A' }}</td>
                    <td>{{ book.category ?? 'Non spécifiée' }}</td>
                    <td>{{ book.author ? book.author.username : 'N/A' }}</td>
                </tr>
                {% else %}
                <tr>
                    <td colspan="4" class="text-center">Aucun livre trouvé pour cette période</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        
        <a href="{{ path('list_books') }}" class="btn btn-secondary">Retour à la liste complète</a>
    </div>
</body>
</html>