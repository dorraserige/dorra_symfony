<!DOCTYPE html>
<html>
<head>
    <title>Auteurs triés par Email</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1>Liste des auteurs triés par Email</h1>
        
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nom d'utilisateur</th>
                    <th>Email</th>
                    <th>Nombre de livres</th>
                </tr>
            </thead>
            <tbody>
                {% for author in authors %}
                <tr>
                    <td>{{ author.id }}</td>
                    <td>{{ author.username }}</td>
                    <td>{{ author.email }}</td>
                    <td>{{ author.nbBooks }}</td>
                </tr>
                {% else %}
                <tr>
                    <td colspan="4" class="text-center">Aucun auteur trouvé</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        
        <a href="{{ path('showAll') }}" class="btn btn-secondary">Retour à la liste complète</a>
    </div>
</body>
</html>